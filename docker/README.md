cd into this directory and `docker compose up -d` to create a docker container for developing pigallery2 extension.  
`./exec.sh` to access the container with bash. Run `npm run build` in the container to generate server.js.
